name: kcd_app
description: "질병분류기호 - KCD 질병코드 검색 앱"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/kcd_sample.json
    - assets/images/kcd_logo.png
    - assets/data/kcd_tree.json
